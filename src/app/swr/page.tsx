// 'use client'

// import { MyLoader } from '@c/loader/MyLoader'
// import { TodoList } from '@c/views/TodoList'
// import useSWR from 'swr'
// import { fetchTodos, fetchTodosPath } from '@/fetcher/todos'

const Page = () => {
  // const { data, isLoading } = useSWR(fetchTodosPath, fetchTodos)
  // if (isLoading) return <MyLoader />
  // if (!data) return <MyLoader />
  // return <TodoList todos={data} />

  return <div>Ok</div>
}

export default Page
